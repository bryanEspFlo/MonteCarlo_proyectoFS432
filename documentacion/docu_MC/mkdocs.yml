site_name: Proyecto FC

theme:
  name: material
  language: es

plugins:
  - search
  - mkdoxy:
      projects:
        cpp:
          src-dirs: "src include"
          doxy-cfg:
            FILE_PATTERNS: "*.cpp *.h*"
            RECURSIVE: True

nav:
  - Inicio: index.md
  - Tutoriales:
      - Calculo de integral con MC en memoria compartida: ejemMCshare.md
      - Calculo de integral con MC en memoria distribuida: ejemDis.md
  - Referencias: 
      - MC en memoria compartida: cpp/classMC.md
      - MC en memoria distribuida: disCode.md
